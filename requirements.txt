# Core requirements
anthropic[bedrock]>=0.39.0  # For Claude API (Bedrock client)
pyboy==2.2.0       # For Pokemon emulator

# Image processing
Pillow             # For image manipulation
numpy>=1.26,<2     # For array operations (pyboy 2.2.0 needs numpy 1.x ABI)